
<% if @restaurants.any? %>
	<ul>
		<% @restaurants.each do |restaurant| %>
			<li><%= restaurant.name %> 
				<%= link_to "Edit", edit_restaurant_path(restaurant) %>
			</li>
		<% end%>
	</ul>
<% else %>
	No restaurants yet
<% end %>